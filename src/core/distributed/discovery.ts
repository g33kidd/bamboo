/**
 * ServiceDiscovery is the "application" for running
 * discovery queries on the client & server. This
 * class helps by managing the client & server.
 *
 * It's also responsible for keeping/updating the
 * current machine's details.
 *
 * It's also responsible for maintaining a list
 * of the services/instances that have been discovered.
 */
export default class ServiceDiscovery {}
